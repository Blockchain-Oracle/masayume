/** The X mention relay actor — registered from main.ts; implemented by the X rail slice. */
export async function startXRelay(log: (why: string) => void): Promise<void> {
  log("x-relay: not implemented yet");
}
