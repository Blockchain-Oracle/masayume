/** The strategy runner actor — registered from main.ts; implemented by the strategies slice. */
export async function startStrategyRunner(log: (why: string) => void): Promise<void> {
  log("strategy-runner: not implemented yet");
}
