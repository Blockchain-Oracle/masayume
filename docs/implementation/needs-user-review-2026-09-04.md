---
title: Deviations awaiting the owner's decision
status: decided — 2026-09-04, all 45 rows KEEP (the owner: "I actually approve of everything")
authority: docs/implementation/parity-ledger.md (every row below is a row there, quoted)
---

# The unreviewed deviations, in one table

**Decided 2026-09-04.** The owner approved every row below as **keep** in one ruling; the ledger's 45 `Needs user review` markers now read `Approved (owner, 2026-09-04)`. Any row may still be named later to revert. The table stays as the record of what was decided.

Every row here was marked **Needs user review** in the parity ledger. Each is a place where Masayume differs from Yosuku on purpose — an adaptation to DreamDEX, an honesty rule, or an addition — that the ledger's own rule says only the owner may approve.

| # | Surface | The deviation | Evidence | Keep / revert |
|---|---|---|---|---|
| 1 | The hero foot's alerts trigger takes the foot's grammar | the Room's 9px mono uppercase text, not the bordered pill from the reference's Share/Copy/Alert row (a row the foot never had) | `app/markets/page.tsx` L758–777 (`.hero-chart-foot`: the Room button and the ramp, nothing else); `PriceAlerts.tsx` L50–56 | |
| 2 | Band presets follow the asset's price. | The reference's ±$15/30/55 and its $5 centre grid are BTC dollars; carried to another asset by its price ratio to the level they were tuned at (`PRESET_ANCHOR_USD` 77,000), the grid snapped to the nearest nice step, edges printed in cents under $10k. Found in a browser: on ETH every band was refused as a near-certainty (ETH 4h Balanced spanned $414, thirteen σ) | `ticket624.core.ts` L222–230; `Ticket624Drawer.tsx` L64–66; context/49 §1 | |
| 3 | The LP Provider badge reads the maker vault's shares | the reference's own `plpBalance > 0` (`lib/badges.ts` L48–52); it stays locked with its dependency named only where no vault is deployed. "Needs Earn (Stage 5)" was false once the vault went live | `lib/badges.ts`; context/49 §9 | |
| 4 | Leverage is a knock-out certificate on the venue's own contracts. | `LeverageReserve` fronts `(L−1)·stake` for a premium, buys the boost off the resting book as the venue's taker and holds it; its claim is repaid first at settlement, at the owner's cash-out, or at the knock-out anyone may trigger once the book's mark is under the maintenance line. The owner's loss is capped at the stake; the reserve's is gap risk at the line, under public caps. The reference's Ticket arithmetic (`tic… | `ticket624.core.ts` `qtyForStake`/`winForQty`; `underwrite.move`; `margin.move`; doc 03 §Leverage; context/45 | |
| 5 | A boost is placed from the wallet only: the reserve buys and | A boost is placed from the wallet only: the reserve buys and custodies the contracts, and `EventVault` has no outbound path by design (AD-5). The higher chips lock off the wallet route with the reference's own treatment for a private bet ("placed at 1x") | `Ticket624Drawer.tsx` L1079–1081; doc 03 "from one Trading Balance" | |
| 6 | The knock-out is permissionless and unpaid (the reference's  | The knock-out is permissionless and unpaid (the reference's margin desk paid its liquidator 5% of proceeds); the house keeper cranks it, and an owner may cash out any time at the bids with a 3% slippage floor under the mark | `margin.move` `liq_penalty_bps`; `LeverageBetRow.tsx` | |
| 7 | The Earn vault is the maker, not a house. | `MarketMakerVault` rests a post-only YES bid and YES ask on the venue's Windows under on-chain bounds (spread, price band, size, per-Window and aggregate caps); it can only buy complete sets at a discount. The reference's vault was the venue's counterparty, which DreamDEX does not have | `app/earn/page.tsx`; doc 03 §Earn/LP; context/44 | |
| 8 | A band's odds are the house's, from the Window's book and the venue's own prints. | `RangeReserve` centres a normal return where the Window's resting book puts the market (Φ⁻¹ of its P(close ≥ open) over the parlay's depth) and widens it by a per-asset σ measured from the venue's closing prints; a band opened mid-Window is therefore priced against the current price. The reference's venue priced bands with its own model; DreamDEX has none | `ticket624.core.ts` §range; `RangeMath.sol`, `RangePricing.sol`; context/43 §What this settles | |
| 9 | `/games/range` is built on the parlay page's frame because t | `/games/range` is built on the parlay page's frame because the reference has no range page; the Ticket's range mode is the reference's own | ledger §RangeReserve | |
| 10 | The range band is not yet drawn on the hero's chart (the ref | The range band is not yet drawn on the hero's chart (the reference shades it); recorded as a follow-up | `Ticket624Drawer.tsx` L750–761 | |
| 11 | The vault has no admin, no pause and no owner; the reference | The vault has no admin, no pause and no owner; the reference's admin-only `write_off_locked_for` has no counterpart | `trading_vault.move` L36, L336; doc 02 "pause does not trap owner exits" | |
| 12 | Reputation tiers ported without the reference's per-tier bon | Reputation tiers ported without the reference's per-tier bonus % and fee % | `lib/predictionContract.ts` TIERS | |
| 13 | Reputation, badges, the equity curve and CSV export are moun | Reputation, badges, the equity curve and CSV export are mounted on `/portfolio` §03 "Your record" | The reference computes all four on its portfolio page (`computeBadges`, `fetchReputation`, `equityRef`, `positionsToCSV`) but its pinned JSX mounts none of them | |
| 14 | `/download` is the PWA install surface: manifest + icons, a  | `/download` is the PWA install surface: manifest + icons, a stateful install CTA (`beforeinstallprompt` / iOS steps / browser menu / installed), the phone frame around a real capture of `/markets`. Native stays Blocked and is said plainly in the meta list | doc 03 §Download; `app/download/page.tsx`, `manifest.json` | |
| 15 | Price alerts get a live evaluator (the reference's `checkAle | Price alerts get a live evaluator (the reference's `checkAlerts` has no caller and its button was cut in 8c7ecc1); mounted in the hero foot beside the Room | doc 03 §Alerts "market stream evaluator"; reference `PriceAlerts.tsx` orphaned | |
| 16 | `/news` is restored from the reference's own history (`93d09 | `/news` is restored from the reference's own history (`93d09c1^:app/news/page.tsx`); the feed component and RSS route survive in the pinned source. "Bitcoin News" → "Market News" because the venue lists BTC and ETH. A More-menu entry is added | doc 03 §News/ticker "real configured feed/provider" | |
| 17 | Public pages rewritten on real facts only: `/docs` (contract | Public pages rewritten on real facts only: `/docs` (contracts from the pinned addresses, code from the repo), `/how-it-works` (the reference's 64/36 example kept and labelled an example; fees truth-corrected), `/pitch` (labels CONCEPT / NEXT / NO-OP / ILLUSTRATIVE; team = the one builder in `git log`; no revenue rate), `/demo` (live traction from `/api/leaderboard`, real screenshots, real tx links) | doc 03 §Public/support surfaces; doc 05 §No fake-data | |
| 18 | `TradingBalanceView`, `VaultControls`, `vault.css` | `TradingBalanceView`, `VaultControls`, `vault.css` | Restored from the reference's own history, as `/news` was | |
| 19 | `VaultGrants`: the live grants and revoke, under the cells | `VaultGrants`: the live grants and revoke, under the cells | Additive — ours | |
| 20 | Enable sheet (caps editor, worked example, capability receip | Enable sheet (caps editor, worked example, capability receipt, one signature via `depositAndGrant`), manager, chip — in the Ticket's ported grammar (`.tk-modes`, `.tk-lev`, `.tk-control-label`) | Adapted — no source | |
| 21 | The `api/claim` flow, ported; the tweet-a-code step says so | The `api/claim` flow, ported; the tweet-a-code step says so | Adapted | |
| 22 | The grant's real caps (per trade, per day, 8 open Windows, 3 | The grant's real caps (per trade, per day, 8 open Windows, 30 days); the proof links point at `/docs` until real transactions exist; "settles back to you" kept because it is true | Truth correction | |
| 23 | What waits is the Trading Balance of the wallet the account  | What waits is the Trading Balance of the wallet the account routes to; the reveal is a public read of that vault; the claim is connecting that wallet, with a signed re-link otherwise | Adapted | |
| 24 | The X betting balance is the EXECUTOR grant's budget; "Cash  | The X betting balance is the EXECUTOR grant's budget; "Cash out" is `revoke`, returning the budget to the Trading Balance (copy says so) | Adapted | |
| 25 | Kept dark (`data-theme="dark"`) beside Reels per doc 04; the | Kept dark (`data-theme="dark"`) beside Reels per doc 04; the sticky strip keeps only the brand and "open the app" | Adapted | |
| 26 | Two signatures: vault `depositAndGrant`, then registry `subs | Two signatures: vault `depositAndGrant`, then registry `subscribe` (which verifies the grant on the vault and pulls the fee to the creator; the registry never holds funds); the copy says so | Truth correction | |
| 27 | Same markup, live: publish a spec the house runner honours | Same markup, live: publish a spec the house runner honours | Adapted | |
| 28 | "Desk balance" is the grant's budget; withdrawing revokes fi | "Desk balance" is the grant's budget; withdrawing revokes first, so it pauses copying | Adapted | |
| 29 | Caps: guarded = 1 open, price ≤ 0.70; balanced = 1 open, ≤ 0 | Caps: guarded = 1 open, price ≤ 0.70; balanced = 1 open, ≤ 0.85; active = 2 open, any price | Adapted — DreamDEX has no leverage | |
| 30 | the same three on BTC to the dollar; other cadences scale by | the same three on BTC to the dollar; other cadences scale by √time from the 5-minute anchor (15m ≈ 1.7, 4h ≈ 6.9, 1d 16); other assets scale by price ratio to the level the dollars were tuned at, with the grid snapped to a nice step and edges in cents under $10k (ETH: ±$0.40/$1.00/$1.80 on $0.20 at 5m) | Adapted — lanes and assets the reference did not have; the price scaling is the decision-log row of 2026-09-03 | |
| 31 | not drawn | the chart card and the Ticket are siblings; lifting the band into the hero is a follow-up | Pending | |
| 32 | `/games/range` with OUTSIDE as a side, the Window picker, th | `/games/range` with OUTSIDE as a side, the Window picker, the slip's Settle / Void (no oracle answer) / Claim pills | Additive — the game the doc-04 mode row names | |
| 33 | not shown (the reference's history endpoints were dead) | not shown (the reference's history endpoints were dead) | §02 "Where the capital is": one row per Window the maker is on — deployed, inventory both sides, state (resting / paired / one-sided / closed), the realized res | |
| 34 | "Withdraw N idle" when capital is deployed; every closed uns | "Withdraw N idle" when capital is deployed; every closed unsettled Window is settled first (the page sends the permissionless crank, one signature each, asking the vault again after each — four were closed at once when the flow ran, context/49) | Adapted | |
| 35 | §02 the Windows table with Merge / Settle; its labels read i | §02 the Windows table with Merge / Settle; its labels read in one round (`useMarketsLite`), "…" only until they land | Additive | |
| 36 | the Trading Balance with a wallet top-up in the same PTB | the Trading Balance with a wallet top-up in the same PTB | the wallet only (the vault has no outbound path, AD-5); the chips lock off the wallet route | |
| 37 | The sentence, then one line on how a knock-out settles; the  | The sentence, then one line on how a knock-out settles; the front, the fee and the line sit in the card's header and bar legend | Additive (truth) | |
| 38 | The multiple only on boosted rows; plain rows say nothing | The multiple only on boosted rows; plain rows say nothing | Adapted — the recorded Stage 2 deviation, resolved | |
| 39 | "Yours now" (mark − front), the line or "at the knock-out li | "Yours now" (mark − front), the line or "at the knock-out line"; no percentage health | Adapted | |
| 40 | an enclave-signed BCS ticket, its key pinned on chain in `ti | an enclave-signed BCS ticket, its key pinned on chain in `ticket_seal::PrivateDesk`, verified locally with ed25519 | an EIP-712 `Claim` signed by the desk key the contract pins, verified locally with `verifyTypedData` against `desk()` | |
| 41 | Private as the route control's third option (Wallet / Tradin | Private as the route control's third option (Wallet / Trading Balance / Private), shown where a desk is deployed, disabled with the reason in its title, "retry" beside it; drops back to the wallet — and says so — when the desk goes away, and silently when the bet is a band | Adapted | |
| 42 | Not flipped | : the option stays chosen and the CTA names the cap (`private-over-cap`); a silent private-to-public flip is the one outcome the route exists to prevent (the code-reviewer agent's finding) | Deviation — safety | |
| 43 | Ported verbatim with its CSS (`private-claims.css`, the refe | Ported verbatim with its CSS (`private-claims.css`, the reference's own light overrides, plus the plate's cream inks); the Window in place of the strike; mounted as the plate's Private pool row panel with the budget controls above it | Restored from the reference's own component, mounted where its pool row is | |
| 44 | The panel's trust and correlation sentences: a stolen desk k | The panel's trust and correlation sentences: a stolen desk key could redirect what an owner allowed it, so the allowance is the blast radius (keep it to a few bets); the charge and the slot's funding stay visible seconds apart | Additive (truth) | |
| 45 | old market page's Share/Copy/Alert row, removed in 8c7ecc1 | old market page's Share/Copy/Alert row, removed in 8c7ecc1 | hero foot, beside the Room | |

45 rows. The ledger's own cells carry the full text of each; this table trims for reading.
