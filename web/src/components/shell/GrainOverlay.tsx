"use client";

/** Film grain over the whole page. Styled by `.grain` in the ported design system. */
export default function GrainOverlay() {
  return <div className="grain" aria-hidden="true" />;
}
