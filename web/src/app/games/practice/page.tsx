import type { Metadata } from "next";
import { CapabilityPending } from "@/components/shell";

export const metadata: Metadata = { title: "Practice" };

export default function Page() {
  return (
    <div className="container">
      <CapabilityPending eyebrow="Practice" title="Practice" dependency="the practice loop over live public prices (Stage 6)">
        <p>The pick loop with no stake and no chain position, so you can learn the motion before any money is involved. Always labelled practice.</p>
      </CapabilityPending>
    </div>
  );
}
