---
scope: Masayume full product build (web app, contracts, ops services, TG rail)
purpose: build-substrate
altitude: feature
updated: 2026-09-01T07:58
---

- (event) Headless create run; inputs: finalized PRD+addendum (locked stack §A, adapter seam §B, contract designs §C, 17-rule canon §D, model §E, numbers §F), UX spines (final), context/01-05, repo scaffold web/ (Next 16.3.4, React 19.2.8, Tailwind v4 — read from package.json)
- (decision) Paradigm: hexagonal (ports & adapters) around ONE chain port, with pure-core engines and single-writer actor services. Binds all; prevents SDK types bleeding upward and parallel chain-access paths
- (decision) AD chain port: only packages/markets imports @somnia-chain/markets-sdk; exposes MarketsProvider (reads/watches) + Submitter (writes); domain types (EventMarket, Quote, OpenPosition, ClosedTrade, LedgerRow) owned by core, adapter maps SDK->domain at the boundary. ADOPTED from PRD addendum B
- (decision) AD money math: all amounts are bigint integer base units end-to-end; token decimals read from chain once at adapter init (6dp tUSDC / 18dp USDso landmine); formatting only at render via one units module; float math in money paths is a defect
- (decision) AD write pipeline: every chain write flows through Submitter's single pipeline — on-chain status===1 gate, fresh quote, IOC + expireTimestampNs from headroom formula, assertTxOk, intent journaled before send, outcomes booked from receipt fills never requested size. Canon addendum D binding. Prevents duplicated tx-build chains (the reference's proven failure)
- (decision) AD wallet-per-role: a checked-in role registry (maker, runner-oracle-follow, relayer, executor, faucet-ops, deployer, demo-user) maps each economic role to a distinct key; one writer per key; self-match rule enforced by construction; house keys exist ONLY in the ops service env, never web
- (decision) AD unified grants (resolves PRD OQ-5): EventVault is the single delegation framework with grant types STRATEGY | EXECUTOR | SESSION; session-key tap-trading = SESSION grant to a browser-held ephemeral viem key (IndexedDB, non-extractable pattern), positions beneficiary hard-wired to owner, caps+expiry in-contract, revoke = one vault call. No EIP-7702 dependency (unverified on testnet); no venue operator registry dependency (spot-only risk). One custody story for all three delegated powers
- (decision) AD client data: RSC shells + client islands; live data via adapter watch subscriptions feeding TanStack Query v5 cache (watch-first, visibility-gated polling fallback); every read crosses the boundary as {value, asOf, stale} envelope — the null!=0 last-good law is a TYPE, not a convention
- (decision) AD server store (resolves PRD OQ-7): single Postgres (Neon serverless) via Drizzle ORM for takes, TG link table, Baku memory, Daily Stop ledger, runner heartbeats/why-strings; zero-env law: every store optional at runtime — absence degrades per EXPERIENCE states (memory-less Baku, empty takes feed, client-local daily stop with honest label); chain truth never in DB (NFR-1)
- (decision) AD services topology: ONE long-running ops service (Node, Railway) hosts every house-key writer as an isolated single-writer actor: seeder/maker, oracle-follow runner, relayer worker, TG executor (grammY), claim sweeper. Web (Vercel) is stateless UI+DB+LLM and calls ops via authenticated internal HTTP; every keeper step keeps its permissionless fallback (NFR-6). Prevents nonce-racing on serverless and key sprawl
- (decision) AD contracts: Foundry in contracts/ (EventVault w/ typed grants, ParlayReserve w/ risk caps + in-tx book read, Waitlist); one generated addresses module consumed by the adapter; the no-divert property and payout-escrow invariant are named forge tests; deploy scripts idempotent
- (decision) AD attribution hook (resolves PRD OQ-3 design): Submitter carries an order-attribution hook (builder tag), no-op v1, so builder codes can land on EC without touching call sites
- (decision) AD Daily Stop authority: Postgres table keyed by wallet, written/read by web ticket API and executor alike; client mirror advisory only; no-DB degradation = per-browser stop with honest scope label
- (decision) AD component kit: shadcn/ui (Radix primitives) skinned via DESIGN.md tokens in Tailwind v4 @theme CSS config; product surfaces (Ticket, Reel, Receipt, plates) hand-built; no second kit. Resolves EXPERIENCE Foundation deferral
- (decision) AD monorepo: pnpm workspaces — web/ (exists), packages/core (pure engines, zero runtime deps except zod), packages/markets (the port), services/ops, contracts/; dependency direction core <- markets <- (web|ops); core imports nothing above it
- (decision) AD errors: Result envelopes at every boundary; one error-map module translates raw provider errors to honest diagnoses (out-of-STT case per NFR-10); ops actors log why-strings every cycle incl. idle heartbeats
- (decision) AD testing: vitest for core engines (FIFO/parlay/sizing/leaderboard golden tests incl. loss-synthesis), forge for contracts (invariant-named tests), ops integration harness = dry gate -> wet gate vs distinct counterparty wallet -> open-order delta leak check, network-free CI invariants script (status enum, taker-IOC, headroom, address/SDK drift)
- (decision) AD deploy/environments: web -> Vercel; ops -> Railway (single service, one region); contracts -> Shannon testnet via forge script; env schema zod-parsed in one env.ts with baked testnet defaults satisfying the zero-env judge quickstart; adapter configured with both testnet RPCs, rotate-on-failure
- (version) Pinned: markets-sdk 0.28.1 EXACT (verified latest, doc 02); next 16.3.4 + react 19.2.8 + tailwind 4 (from repo); wagmi v2 + RainbowKit (verified via Context7 2026-08-31, direction doc); ai v6 + AI Gateway, model env-swappable (same); TanStack Query v5; Drizzle ORM; grammY for TG bot; Foundry stable
- (assumption) Railway chosen for ops host (any long-running Node host works; the AD binds the single-writer topology, not the vendor)
- (assumption) Neon chosen for Postgres (zero-env law makes the vendor swappable; Drizzle isolates dialect)
- (assumption) grammY chosen as TG bot framework (mainstream, long-polling — no public webhook needed on ops host)
- (question) OQ-10 (testnet reference anomaly) is an empirical check in build phase 1 — architecture carries the seam (model reads settlement-basis price via adapter) but cannot resolve the anomaly from documents
- (event) Reviewer Gate complete: lint 0 findings (x2); rubric conditional-pass; versions pass-with-corrections; adversarial CONDITIONAL 14 findings; 2 reconcilers. All findings applied in one revision.
- (change) Adversarial fixes: AD-3 two lanes (submitOrder/submitTx, no writeContract outside markets); AD-9 rewritten (closed 5-entry-point set, Stop as mandatory pipeline step, atomic checkAndReserve in ops, spend semantics + IANA tz pinned, 'web ticket API' retired); AD-5 appends (SESSION spends Vault — deposit+grant composed, Vault-labeled sizing, funds-exceeded fallback; UTC-day cap clock + core/caps.simulate); new AD-14 I/O boundary (fetch/wagmi holes closed); new AD-15 claim protocol (relayer sole authority, per-item intents/results, core/claims shared); AD-10 append (on-chain market id in contract storage, never pool address); AD-1 appends (getLedger sole history feed, lifecycle enum, nextWindow); conventions: 3-domain time suffixes, integer probabilities + parlay golden vectors, one-writer-per-table, Reading<T> composed envelope
- (change) Rubric fixes: new AD-16 identity/auth (SIWE sessions, TG signed link proof, bearer+HMAC internal HTTP); judge ring homed in seed+map (/dev fixtures, /dev/doctor, /demo, /pitch no-live-data); ops envelope completed (watchdog TG alerting, Neon backup posture, deploy lockstep, Railway trial note); cost/abuse controls degrade closed; zero-env scoped to local/judge runs (prod requires DB)
- (version) Corrections from version lens: ai v6 -> v7 (current major; Gateway docs target v7; PRD addendum patched); wagmi v2 = RainbowKit peer-req pin with stated fallback; drizzle-orm >=0.40.1 + neon serverless 1.1.x; Railway trial-credit note
- (change) Upstream syncs applied: PRD FR-29 now enumerates the Session grant type (AD-5); PRD addendum A copilot row -> ai v7; addendum A app-frame row -> relayer lives in services/ops per AD-8
- (event) spine finalized
- (change) Pre-build package review (4 lenses, 85+ findings) applied 2026-09-01 — full dispositions in _bmad-output/planning-artifacts/reviews/package-review-2026-09-01.md. Key changes here: see that report's build-blocking + per-artifact sections.
